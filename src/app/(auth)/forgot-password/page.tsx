import { ForgotPasswordForm } from "./_components/forgotPassword-form"
export default function ForgotPasswordPage() {
    return (
        <main className="w-full h-dvh relative">
            <ForgotPasswordForm className="w-full max-w-md flex items-center justify-center absolute top-1/2 left-1/2 -translate-y-1/2 -translate-x-1/2" />
        </main>
    )
}